<% current_user.projects.each do |project| %>
  <div class="project">
    <%= link_to project.title, project_path(project) %></span>
    <%= link_to "delete", project, method: :delete,
      data: { confirm: "You sure?" },
      :class => "delete right", remote: true %>
  </div>
<% end %>
